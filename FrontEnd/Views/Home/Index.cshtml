@*@model List<ManagementGamesDTO.Tournament>*@
@*@model ManagementGamesDTO.Tournament*@
@model FrontEnd.Models.Test;

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <ul>
                <li><a asp-page="TournamentForm">Зарегистрировать турнир</a></li>
                <li><a asp-page="ParticipantForm">Зарегистрировать участника</a></li>
            </ul>
        </div>
        <div class="col-md-4">
            <h1>Турниры</h1>
            <table>
                <tr>
                    <th>ID</th>
                    <th>Название</th>
                    <th>Дата</th>
                    <th>Время начала</th>
                    <th>Время окончания</th>
                    <th>Формат</th>
                    <th>Тип</th>
                    <th>Место проведения</th>
                    <th>Сетка турнира</th>
                    @*</tr>
                        @foreach (var tournament in Model)
                        {
                            <tr>
                                <td>@tournament.TournamentId</td>
                                <td>@tournament.TournamentName</td>
                                <td>@tournament.Date.</td>
                                <td>@tournament.StartTime.</td>
                                <td>@tournament.EndTime.</td>
                                <td>@tournament.TournamentFormat</td>
                                <td>@tournament.TournamentType</td>
                                <td>@tournament.TournamentLocation</td>
                                <td>@tournament.TournamentGrid</td>
                                <td><a asp-page="TournamentDetails" asp-route-id="@tournament.TournamentId">Редактировать</a></td>
                                <td><a>Удалить</a></td>
                            </tr>
                        }*@

                <tr>
                    <td>@Model.Id</td>
                    <td>@Model.Name</td>
                    @*<td>@Model.Date.</td>
                        <td>@Model.StartTime.</td>
                        <td>@Model.EndTime.</td>
                        <td>@Model.TournamentFormat</td>
                        <td>@Model.TournamentType</td>
                        <td>@Model.TournamentLocation</td>
                        <td>@Model.TournamentGrid</td>
                        <td><a asp-page="TournamentDetails" asp-route-id="@Model.TournamentId">Редактировать</a></td>
                        <td><a>Удалить</a></td>*@
                </tr>

            </table>
        </div>
    </div>
</div>

